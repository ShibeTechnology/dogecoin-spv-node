const doubleHash = require('./doubleHash')
const decodeAddress = require('./decodeAddress')
const decodeHeader = require('./decodeHeader')

module.exports = {
  decodeAddress,
  decodeHeader,
  doubleHash
}
